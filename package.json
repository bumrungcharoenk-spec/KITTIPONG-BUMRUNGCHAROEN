{
  "name": "navify",
  "version": "1.0.0",
  "private": true,
  "description": "Navify campus navigation prototype",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:assets": "node build-assets.js",
    "dev": "npm run build:assets && wrangler dev",
    "start": "npm run dev",
    "deploy": "npm run build:assets && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.41.0"
  }
}
